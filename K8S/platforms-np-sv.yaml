apiVersion: v1
kind: Service
metadata:
  name: platformmnpservice-srv
spec:
  type: NodePort
  selector:
    app: platformservice
  ports:
    - name: platformserice
      protocol: TCP
      port: 8080
      targetPort: 8080